{
  "scripts": {
    "compile": "bun build src/index.js src/pre.js src/post.js --minify --outdir=dist --target=node --format=cjs --sourcemap=external"
  },
  "dependencies": {
    "@actions/cache": "^5.0.3",
    "@actions/core": "^2.0.2",
    "@actions/github": "^8.0.0",
    "@actions/tool-cache": "^3.0.0",
    "string-argv": "^0.3.2"
  }
}